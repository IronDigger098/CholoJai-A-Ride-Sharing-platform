# Every pull request requests a review from the owner of the files it
# touches. On a solo project this is documentation of responsibility rather
# than a gate — but it is the mechanism a growing team relies on, and
# setting it up before the second contributor arrives is cheaper than after.

*                       @IronDigger098

# Areas where a change has consequences beyond the file being edited.
# Listing them separately makes the blast radius explicit to a reviewer.

/docs/                  @IronDigger098
/.github/               @IronDigger098
/apps/api/prisma/       @IronDigger098
/packages/shared/       @IronDigger098
